<% provide(:title, "注文情報入力") %>
<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h2 class="text-center">注文情報入力</h2>
      <div class="col-xs-offset-1">
        <% if current_customer.cart_items.present? %>
        <%= form_with model:@order, url: public_orders_confirm_path, local: true do |f| %>
        <h3 class="col-xs-3 sub-head text-center">
          <i class="fas fa-yen-sign"></i>支払い方法</h3>
            <br>
              <%= f.radio_button :payment_method, "credit_card" %>
              <%= f.label :payment_method, "クレジットカード" %>
              <%= f.radio_button :payment_method, "transfer" %>
              <%= f.label :payment_method, "銀行振込"%>
            </br>
          <h3 class="col-xs-3 sub-head text-center"><i class="fas fa-people-carry"></i>お届け先</h3>
            <br>
              <%= f.radio_button :address_number, "1" %>
              <%= f.label :address_number_1, "ご自身の住所" %>
              <%= "#{current_customer.post_code} #{current_customer.address}" %>
              <%= "#{current_customer.first_name} #{current_customer.last_name}" %>
            </br>
              <%= f.radio_button :address_number, "2" %>
              <%= f.label :address_number_2, "登録先の住所" %>
                <span>登録済住所から選択</span><br>
                <%= f.collection_select(:registered, current_customer.addresses.all, :id, :address) %>
            </br>
              <span>新しいお届け先</span><br>
              <%= f.radio_button :address_number, 3 %>
              <%= f.label :address_number_3, "新しいお届け先" %>
              <%= f.label :post_code, "郵便番号" %>
              <%= f.text_field :post_code %>
              <%= f.label :address, "住所" %>
              <%= f.text_field :address %>
              <%= f.label :name, "名前" %>
              <%= f.text_field :name %>
            </br>
            <%= link_to public_cart_items_path, class: "glyphicon glyphicon-shopping-cart btn btn-info cart-back" do %>
              カート(<%= current_customer.cart_items.count %>)へ戻る
            <% end %>
            <%= f.submit "確認画面へ進む", class: "btn btn-success col-xs-5" %>
        <% end %>
        <% else %>
          <p>カートに商品がない為、注文情報の入力ができません。</p>
          <p><%= link_to "商品一覧", public_products_path, class: "btn btn-warning" %>から商品をお探し下さい。</p>
        <% end %>
      </div>
    </div>
  </div>
</div>